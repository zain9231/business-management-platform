# Scripts

`quality.py` provides the repository's Python `format`, `lint`, and `typecheck` commands.
Run it from the backend development environment using the commands in
[the main README](../README.md#quality-checks).

Demo-data seeding and DBML-to-migration schema-parity scripts are added when their owning backlog
tasks begin.
